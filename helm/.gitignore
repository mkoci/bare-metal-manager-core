# Helm dependency packaged artifacts (regenerated by `helm dependency build`)
# NOTE: Chart.lock is NOT ignored — it should be committed for reproducible builds.
charts/*.tgz

# Backup files
*.backup

# OS files
.DS_Store
